# Lint and type check
npx tsc --noEmit  # Runs TypeScript type checking and ESLint with auto-fix

# Create a migration file when creating a new table or column
Create a migration file when creating a new table or column

# Security
Do every action with security in mind